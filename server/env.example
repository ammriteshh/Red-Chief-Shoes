    # Environment Variables for Red Chief Shoes Backend
    # Copy this file to .env and update the values

    # Server Configuration
    NODE_ENV=development
    PORT=5000

    # Database Configuration
    MONGODB_URI=mongodb://localhost:27017/redchief

    # JWT Configuration
    JWT_SECRET=red-chief-shoes-super-secret-jwt-key-2024-change-in-production
    JWT_EXPIRE=7d

    # File Upload Configuration
    MAX_FILE_SIZE=1000000
    UPLOAD_PATH=uploads/

    # Email Configuration (for future use)
    EMAIL_HOST=smtp.gmail.com
    EMAIL_PORT=587
    EMAIL_USER=your-email@gmail.com
    EMAIL_PASS=your-app-password

    # Payment Gateway Configuration (for future use)
    STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
    STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

    # Frontend URL (for CORS)
    FRONTEND_URL=http://localhost:5173

    # Admin Configuration
    ADMIN_EMAIL=amritesh6767_db_user
    ADMIN_PASSWORD=NAo97L8xwLG36fQa
